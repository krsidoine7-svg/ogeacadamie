# Configuration Locust — OGE Académie
# Serveur local Next.js (npm run dev)
host = http://localhost:3000

# Fichier de scénario par défaut
locustfile = scenarios/oge_public_test.py

# Paramètres par défaut
users = 20
spawn-rate = 2
# run-time = 5m
